/*Accounts.onCreateUser(function () {
  // create first admin user, if none
  if (Meteor.users.find() === 0 ) {
    user.profile = options.profile || {};
    user.profile.role = 'admin';
  }

  return user;
});*/